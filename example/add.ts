/**
 * The add() function is used to demonstrate the inclusion
 * of an example to the documentation.
 *
 * @includeExample ./example/add.example.ts
 */
export function add(a: number, b: number): number {
	return a + b;
}
